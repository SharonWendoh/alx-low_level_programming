#include <stdio.h>
#include "main.h"
/**
 * add - adds two integers and prints result
 *@n: input number as an integer.
 *@m: input no 2
 *
 * Return: The sum
 */
int add(int n, int m)
{
	int s;

	s = m + n;
	return (s);
}
